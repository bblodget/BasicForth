# Build artifacts
*.o
version.inc
src/arch/arm64/basicforth
src/arch/x86/basicforth
src/arch/arm64/test_basicforth
src/arch/x86/test_basicforth

# Copied core.fs in build directories
src/arch/arm64/core.fs
src/arch/x86/core.fs

# Redirect/output artifacts from running the example scripts
examples/*.txt

# Local deploy scripts (see deploy_template.sh)
deploy.sh

# Pumpkin related
pumpkin-scripts/

# temporty testig directory
testing/
__pycache__/

# Local Python virtualenv (a TTS engine for voice.fs lives here) — 250 MB of
# machine-specific binaries, and the path is baked into setup.sh
venv/
